# ExampleUsingOfSpring4
внедрение зависимостей не через конструктор (ExampleUsingOfSpring3), а через setter, который берёт значение по ссылке в конфигур. файле "<property name="music" ref="musicBean"/>" 
